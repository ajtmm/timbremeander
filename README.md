# Timbre Meander

A wandering randomizer of poems. Drop a markdown file in `poems/`, add it to the manifest, and the site picks one at random on every visit (or button press).

Built with vanilla HTML / CSS / JS — no build step, no framework, no server. Host it on GitHub Pages and you are done.
